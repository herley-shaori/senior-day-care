<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/gambarSenja"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/logoSenja"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/senja_logo"
        android:layout_centerHorizontal="true"/>

    <EditText
        android:id="@+id/emailInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Email Anda"
        android:inputType="textPersonName"
        android:text="alen@gmail.com"
        android:textSize="24dp"
        android:layout_centerInParent="true"/>

    <EditText
        android:id="@+id/passwordInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Kata Sandi Anda"
        android:inputType="textPassword"
        android:text="rahasia"
        android:textSize="24dp"
        android:layout_centerInParent="true"
        android:layout_below="@id/emailInput"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/passwordInput">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_centerInParent="true">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/registerButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/daftar_button_background"
                android:enabled="false"
                android:text="Daftar"
                android:layout_margin="10dp"
                android:layout_weight="0.5"/>

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/loginButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:background="@drawable/login_button_background"
                android:enabled="true"
                android:layout_weight="0.5"
                android:text="Login"
                android:layout_margin="10dp"
                />

        </LinearLayout>

    </RelativeLayout>

</RelativeLayout>